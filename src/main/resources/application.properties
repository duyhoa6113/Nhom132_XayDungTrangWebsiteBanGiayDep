spring.application.name=Nhom132_XayDungTrangWebsiteBanGiayDep

# ========== DATABASE CONFIGURATION ==========
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=nhom132_shoponline;encrypt=false
spring.datasource.username=sa
spring.datasource.password=123

# ========== JPA/HIBERNATE CONFIGURATION ==========
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ========== SESSION SECURITY ==========
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# ========== EMAIL CONFIGURATION (GMAIL) ==========
# SMTP Server - Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# Gmail Account Credentials
spring.mail.username=lyducq132@gmail.com
spring.mail.password=qjrm pxwr ghiv wmbu

# SMTP Protocol & Properties
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email Connection Timeout
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email Encoding
spring.mail.default-encoding=UTF-8

# Enable Email Debugging (Optional - for troubleshooting)
spring.mail.properties.mail.debug=false

# ========== SCHEDULING CONFIGURATION ==========
spring.task.scheduling.enabled=true

# T?t cache Thymeleaf ?? server luôn ??c file m?i
spring.thymeleaf.cache=false

# Upload configuration
upload.path=src/main/resources/static/uploads/avatars

# Static resources
spring.web.resources.static-locations=classpath:/static/,file:src/main/resources/static/

# File upload size
spring.servlet.multipart.max-file-size=1MB
spring.servlet.multipart.max-request-size=1MB